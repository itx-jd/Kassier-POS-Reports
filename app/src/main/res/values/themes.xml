<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KassierPOSLatest" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryVariant">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gray</item>
        <item name="colorSecondaryVariant">@color/gray</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/red</item>
        <!-- Customize your theme here. -->

        <item name="android:fontFamily">@font/open_sans_regular</item>

        <!-- Define the colorControlActivated attribute to change the radio button indicator color when selected -->
        <item name="colorControlNormal">@color/gray</item>

        <!-- Define the colorControlActivated attribute to change the radio button indicator color -->
        <item name="colorControlActivated">@color/red</item>
    </style>
    <style name="NavigationMenuItemStyle">
        <item name="android:textStyle">normal</item>
    </style>
    <style name="Theme.KassierPOSLatest" parent="Base.Theme.KassierPOSLatest" />
</resources>